{
  "name": "scanpower-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for ScanPower API integration",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "watch": "tsx watch src/index.ts",
    "test": "tsx src/test-server.ts",
    "test:build": "npm run build && node dist/test-server.js",
    "install-server": "./install.sh"
  },
  "keywords": ["mcp", "scanpower", "api", "amazon", "fba"],
  "author": "ScanPower MCP Server",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.4.0",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
